import { describe, it, expect } from 'vitest'

describe('Example Test Suite', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })

  it('can do math', () => {
    expect(1 + 1).toBe(2)
  })
})
